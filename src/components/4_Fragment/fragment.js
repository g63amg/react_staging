import React, { Component,Fragment } from 'react'

export default class Demo extends Component {
  render() {
    return (
      <Fragment>fragment</Fragment>
    //   Fragment可以代替div，Fragment不在页面里显示，Fragment只允许有一个key值
    )
  }
}
